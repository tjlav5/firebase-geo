{
  "name": "firebase-geo",
  "version": "0.1.0",
  "description": "",
  "main": "functions/lib/index.js",
  "scripts": {
    "build": "cd functions && npm run build",
    "start": "concurrently \"npm:start:*\"",
    "start:emulators": "firebase ext:dev:emulators:start --project=geo-test --test-config=firebase.json --test-params=.env",
    "start:client": "cd functions && npm run dev",
    "exec": "firebase ext:dev:emulators:exec --project=geo-test --test-config=firebase.json --test-params=.env \"$npm_config_cmd\"",
    "exec:test": "npm run exec --cmd='cd functions && npm run test:dev'",
    "exec:coverage": "npm run exec --cmd='cd functions && npm run test:coverage'"
  },
  "author": "tjlav5",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "firebase-tools": "^8.9.2",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "ngeohash": "^0.6.3"
  }
}
